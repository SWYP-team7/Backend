spring:
  datasource:
#    url: jdbc:mysql://localhost:3306/swyp?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
#    username: root
#    password: 1234
    url: ${db.url}
    username: ${db.username}
    password: ${db.password}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jwt:
    access-key: dev-access-secret-key-that-is-long-enough
